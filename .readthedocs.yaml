version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "mambaforge-4.10"

conda:
  environment: docs/environment.yml

submodules:
  include: all
  recursive: true

sphinx:
   configuration: docs/conf.py
